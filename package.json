{
  "name": "daily-task-manager-supabase-vercel",
  "version": "2.0.0",
  "private": true,
  "description": "Daily Task Manager converted from PHP/SQLite to Vercel serverless + Supabase while preserving the existing UI and Google Sheet connector.",
  "scripts": {
    "check": "node --check api/router.js && node --check api/_lib/auth.js && node --check api/_lib/db.js && node --check api/_lib/googleSheet.js && node --check api/_lib/utils.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "bcryptjs": "^2.4.3",
    "csv-parse": "^5.5.6",
    "jsonwebtoken": "^9.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
